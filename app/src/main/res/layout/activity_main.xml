<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="#FAFAFA">

    <!-- Header minimalista -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="32dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="NoGracias (BETA)"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#2C3E50" />

        <!-- Switch principal -->
        <Switch
            android:id="@+id/switchMain"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="true"
            android:textOff="OFF"
            android:textOn="ON"
            android:thumbTint="#4CAF50"
            android:trackTint="#E8F5E8" />

    </LinearLayout>

    <!-- Estado -->
    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="✅ Bloqueo activo"
        android:textSize="16sp"
        android:textColor="#4CAF50"
        android:gravity="center"
        android:layout_marginBottom="24dp"
        android:padding="12dp"
        android:background="#F1F8E9"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp" />

    <!-- Agregar prefijo -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="24dp"
        android:background="#FFFFFF"
        android:elevation="2dp"
        android:padding="4dp">

        <EditText
            android:id="@+id/etPrefix"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Add prefix..."
            android:inputType="phone"
            android:maxLines="1" />

        <Button
            android:id="@+id/btnAdd"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:text="+"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:background="#2196F3"
            android:minWidth="48dp"
            android:layout_margin="2dp" />

    </LinearLayout>

    <!-- Lista de prefijos CON LISTVIEW -->
    <ListView
        android:id="@+id/lvPrefixes"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp" />

    <!-- Mensaje cuando está vacío -->
    <TextView
        android:id="@+id/tvEmpty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="No hay prefijos bloqueados\n\nAgrega un prefijo numérico para comenzar"
        android:textSize="16sp"
        android:textColor="#757575"
        android:gravity="center"
        android:visibility="gone" />

</LinearLayout>